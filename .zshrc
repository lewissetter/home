#
# ~/.bashrc
#

# If not running interactively, don't do anything
[[ $- != *i* ]] && return

alias ls='ls --color=auto'
PS1='\W\$ '

alias ll='ls -la'

alias printing='firefox http://localhost:631/'

alias f='firefox'

alias matlab='/usr/local/MATLAB/R2018a/bin/matlab'
